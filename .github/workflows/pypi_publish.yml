name: Release to PyPI

on:
  workflow_dispatch:  # Trigger the workflow manually

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools wheel twine
      
      - name: Install package dependencies
        run: |
          python -m pip install --upgrade pip 
          cd package 
          python setup.py install 
      
      - name: Install promptflow dependencies
        run: | 
          python -m pip install promptflow
          python -m pip install promptflow-tools

      - name: Build package
        run: |
          python package/setup.py sdist bdist_wheel
          sleep 3  # Wait 1 second for files to be created

      - name: Upload to PyPI
        run: |
          twine upload package/dist/* --verbose
        env:
          TWINE_USERNAME: __token__  # PyPI username (usually '__token__')
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}  # PyPI token stored in GitHub secrets
